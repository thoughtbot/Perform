machine:
  xcode:
    version: 8.0
  environment:
    NSUnbufferedIO: YES

dependencies:
  override:
    - gem install cocoapods
    - pod install
    - bin/bootstrap-if-needed
  cache_directories:
    - "Carthage"

test:
  post:
    - pod lib lint
