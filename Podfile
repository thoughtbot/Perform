# This Podfile is not intended for development, but provided to encourage
# Circle CI to properly configure CocoaPods for testing purposes.

platform :ios, "8.0"

install! "cocoapods", integrate_targets: false

target "Perform" do
  podspec
end
